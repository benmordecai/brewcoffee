import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mordecode.vibe.brewcoffee',
  appName: 'BrewCoffee',
  webDir: 'dist'
};

export default config;
